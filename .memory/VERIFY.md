# VERIFY — ecosystem-kit — open acceptance checks

Format: `- [ ] item — how to verify`

- [x] install.sh end-to-end against the REAL engine+templates — verified 2026-07-23 against scratch git repos with ALL FIVE profiles: full `.claude/` (15 hook modules, 10 commands, 6 agents, 7 skills) + `.memory/` layout, tests/ not installed, re-run a no-op (0 seeded / 9 untouched, 0 gitignore lines re-appended), edited `.memory/STATE.md` and tuned `kit.json` byte-identical after re-run. Error paths verified: non-git dir refused, kit-into-itself refused, unknown profile lists available ones.
- [x] update.sh refreshes engine+skills only — verified 2026-07-23 against the real kit: drifted hook + skill refreshed, project-customized command and `.memory/`/`kit.json` byte-identical, non-kit hook left in place with a NOTE, changed files listed (0 new, 2 updated, 20 unchanged).
- [x] health-check.sh on a fresh real install — verified 2026-07-23: wiring parity OK, roster complete, drift detection fires (unwired module named, exit 1). Only ERR on this machine is the true-positive `~/.claude/settings.local.json` leak (see ISSUES).
- [ ] session_boot additionalContext appears at session start in a real project (mylantite first) — restart session, confirm STATE head + VERIFY/ISSUES counts + diary tail + always_load instruction in context. *2026-08-01 hygiene: prerequisite now met — kit installed in all five registered repos and live-session hook artifacts (tdd_state/pending/context_attach) exist in mylantite+meritick caches, so the engine runs in real sessions; the banner CONTENT still needs an interactive session to confirm.* *2026-08-03 hygiene: still not checkable — this run is headless and sandboxed to this repo (confirmed: `~/.claude/*` reads are blocked outright, not just cross-repo checkouts), same gap as 08-01, no new info.* *2026-08-10 hygiene: still open, still needs an interactive session — this run was explicitly scoped to `.memory/` only, no attempt made, no new info.*
- [ ] docs_contract Stop-gate blocks until CHANGELOG/diary updated and honors stop_hook_active — edit a source-pattern file, try to stop, confirm block then release. *2026-08-01 hygiene: not checkable read-only (needs a source-file edit + Stop attempt); logic is unit-covered by test_docs_contract.py in the 94/94 suite — behavioral pass still owed.* *2026-08-03 hygiene: still owed; this run's engine suite is 164/164 (test_docs_contract.py included), but a live Stop-attempt pass is still unrun. Not attempted here — a real source edit + Stop is a mutating, non-read-only action outside this run's remit.* *2026-08-10 hygiene: still owed, same reason — engine suite still 164/164, no live Stop-attempt pass run this week either.*
- [x] guard_protected_merge blocks: `git merge` into main, `gh pr merge`, `git push` refspec targeting main, github MCP write tools on protected branches. — verified 2026-08-01 by piping stdin-JSON fixtures at the real hook from a checkout sitting on main: `git merge` on main, `gh pr merge`, `git push origin HEAD:main`, MCP `merge_pull_request`, and MCP `create_or_update_file` with branch=main all exit 2 with BLOCKED reasons; control (`git push origin HEAD:feature/x`) exits 0. 6/6.
- [x] Engine unittest suite passes on host: `python3 -m unittest discover engine/hooks/tests` — 164 tests OK, re-verified 2026-08-10 (unchanged since 2026-08-03; was 90 on 2026-07-23; suite has grown alongside the shell-splitter, diary-gate, and PR-thread work — see CHANGELOG).
- [ ] Fail-open/fail-closed split behaves: break an advisory hook, confirm tools still run with a stderr warning; break `secret_scanner`, confirm Edit/Write blocked (exit 2). *2026-08-01 hygiene: not checkable read-only (requires deliberately breaking hook files); needs a scratch-install behavioral pass.* *2026-08-03 hygiene: still open, same reason — deliberately breaking a hook file is a mutation this headless run does not perform.* *2026-08-10 hygiene: still open, same reason, no new info.*
