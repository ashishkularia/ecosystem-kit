# ISSUES — ecosystem-kit

Format: `- [ ] YYYY-MM-DD — description (context)`

- [ ] 2026-07-23 — `update.sh` refreshes `templates/skills/` into targets by overwrite; a project-customized skill file gets replaced (changed files are listed in the output, but not backed up). Decide whether skills need a skip-if-locally-modified mode or move customizable skills out of the refresh set.
- [x] 2026-07-23 — RESOLVED (verification pass, same day): `.memory/cache/.gitignore` now contains `*` + `!.gitignore` and the root-level `.memory/cache/` ignore line is gone from the snippet (and scrubbed by install.sh where a previous install appended it), so the `.gitignore` is tracked and `cache/` survives clones. install.sh also seeds `.gitkeep` into empty `diary/`/`auto/`; health-check downgrades a missing `cache/` to WARN (hooks recreate it on demand).
- [ ] 2026-07-23 — Daemon cannot run when the repo path is deep: AF_UNIX sun_path limit (~107 chars) makes `_daemon.py` disable itself for nested checkouts (worktrees, CI workspaces, scratch rehearsals); clients silently pay direct-exec cost on every hook call and health-check always WARNs. Graceful degradation verified safe (fail-open/fail-closed semantics hold). Optional hardening: fall back to a short hashed socket path like `/tmp/ck-<md5(PROJECT_ROOT)[:12]>.sock` with an owning-root handshake. The five named project repos live at `/home/ubuntu/<repo>` and are unaffected.
- [ ] 2026-07-23 — The kit repo itself has ZERO commits, so `.claude/kit-version` in targets stamps "kit commit unknown" and provenance is lost. Needs the initial dog-food commit — but Claude never commits to main/master (owner-only rule), and an unborn `main` is exactly that. OWNER ACTION: make the initial commit (or tell Claude to commit on a work branch you then merge).
- [ ] 2026-07-23 — `update.sh` stamps kit-version from `kit.config.example.json` falling back to the target's `kit.json` — if the kit bumps its version only in profiles, updated targets keep the old stamp. Consider a single VERSION source in the kit root.
- [ ] 2026-07-23 — MIGRATION BLOCKER on this machine: `~/.claude/settings.local.json` still carries 23 `_client.py` hook wirings pointing at the legacy global `~/.claude/hooks/` copy of the old DevContainer ecosystem. `health-check.sh` correctly flags this as ERR in EVERY installed repo (project hook wiring must live only in `<repo>/.claude/settings.json`). Remove the `hooks` block from `~/.claude/settings.local.json` during rollout — keep the machine-level `hooks-machine/guard_protected_branch.py` wiring, which is allowed and not flagged.
