# ISSUES — ecosystem-kit

Format: `- [ ] YYYY-MM-DD — description (context)`

- [x] 2026-08-01 — RESOLVED (same day): the kit now runs on itself — `.claude/kit.json` from `profiles/ecosystem-kit.json`, and `.claude/settings.json` wiring the full hook roster at `engine/hooks/_client.py` (the LIVE source; a copy would drift from the engine under development). `health-check.sh` derives the hooks dir from the wiring, so one script covers both layouts. Self-check went 55%/6 ERRs -> 100%/21 checks. Running it on itself immediately found a real bug (daemon staleness ignored `kit.json`).
- [ ] 2026-08-01 — Machine-wide daemon sprawl: 10+ `_daemon.py` processes are alive from repos and worktrees, some since 2026-07-23, several for paths that no longer exist (`/tmp/tmp.6OmwgctqXx`, deleted worktrees). Each holds hook code from its start date. The 2026-08-01 staleness fix retires a daemon the moment its own hook files change, so these will now age out on first use — but a daemon for a DELETED path never gets a request and never exits. Consider a reaper in `prune-stale-branches` (or the daemon self-exiting after N idle hours). *(Observed while testing the staleness changeover.)*
- [ ] 2026-07-23 — `update.sh` refreshes `templates/skills/` into targets by overwrite; a project-customized skill file gets replaced (changed files are listed in the output, but not backed up). Decide whether skills need a skip-if-locally-modified mode or move customizable skills out of the refresh set. *(2026-08-01 hygiene: urgency raised — kit-propagate now runs update.sh daily against every registered repo, so a customized skill would be silently clobbered inside an auto-PR; owner review is the only backstop.)*
- [x] 2026-07-23 — RESOLVED (verification pass, same day): `.memory/cache/.gitignore` now contains `*` + `!.gitignore` and the root-level `.memory/cache/` ignore line is gone from the snippet (and scrubbed by install.sh where a previous install appended it), so the `.gitignore` is tracked and `cache/` survives clones. install.sh also seeds `.gitkeep` into empty `diary/`/`auto/`; health-check downgrades a missing `cache/` to WARN (hooks recreate it on demand).
- [ ] 2026-07-23 — Daemon cannot run when the repo path is deep: AF_UNIX sun_path limit (~107 chars) makes `_daemon.py` disable itself for nested checkouts (worktrees, CI workspaces, scratch rehearsals); clients silently pay direct-exec cost on every hook call and health-check always WARNs. Graceful degradation verified safe (fail-open/fail-closed semantics hold). Optional hardening: fall back to a short hashed socket path like `/tmp/ck-<md5(PROJECT_ROOT)[:12]>.sock` with an owning-root handshake. *(2026-08-01 hygiene: registered repos actually live nested — `/home/ubuntu/DevContainer/<repo>` + `/home/ubuntu/homeassistant` — but socket paths are still ~60 chars, well under the limit; all five unaffected.)*
- [x] 2026-07-23 — RESOLVED (2026-08-01 hygiene): the kit repo has real history (HEAD 5d9dd32, clean main tracking origin) and every installed target's `.claude/kit-version` stamps a real kit commit (7e0d2d8 / 59916b5) — provenance restored. *(Was: ZERO commits, kit-version stamped "kit commit unknown".)*
- [ ] 2026-07-23 — `update.sh` stamps kit-version from `kit.config.example.json` falling back to the target's `kit.json` — if the kit bumps its version only in profiles, updated targets keep the old stamp. Consider a single VERSION source in the kit root.
- [x] 2026-07-23 — RESOLVED (2026-08-01 hygiene): `~/.claude/settings.local.json` now carries ZERO legacy `_client.py` wirings — its hooks block holds only the allowed machine-level PreToolUse guard — and all five registered repos wire hooks in-repo via `$CLAUDE_PROJECT_DIR`. Rollout completed. *(Was: 23 legacy global wirings blocking migration, flagged ERR by health-check in every installed repo.)*
- [ ] 2026-08-03 — Headless weekly-hygiene sessions are hard-sandboxed to this repo's own working directory: `cat ~/.claude/repo-registry` and any read touching `~/.claude/*` are blocked outright by the harness ("Claude Code may only concatenate files from the allowed working directories for this session"), not just writes. This means every cross-repo/machine-layer claim in STATE.md (five-repo rollout, live crons, `settings.local.json` wiring) can only be re-verified from an interactive session or one explicitly scoped to `~`, never from a routine headless `.memory/` hygiene run — those claims will silently go stale between real verifications unless something else re-checks them. Consider either widening the hygiene cron's allowed paths (read-only) or adding an explicit "last cross-repo verification" datestamp separate from STATE's general "Last validated" so staleness there is visible instead of implied. *(Observed while trying to re-check STATE.md's machine-layer bullet during this run.)* *2026-08-10 hygiene: still the case — this run was explicitly scoped to `.memory/` only per its instructions, so no attempt was made to re-test the sandbox boundary; no new info either way.* *2026-08-17 hygiene: re-tested — `cat ~/.claude/repo-registry` still blocked outright by the harness with the same "allowed working directories" message. Sandbox boundary unchanged, still no widening; cross-repo claims in STATE.md remain frozen at 2026-08-01.*
