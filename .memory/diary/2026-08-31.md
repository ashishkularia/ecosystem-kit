# 2026-08-31 — weekly hygiene

Headless run. Unlike the previous two runs, both the repo AND the sandbox
had real news: 13 substantive commits landed 2026-08-27→08-30 (first
non-quiet week since 2026-08-17), and the `~/.claude/*` read block that
froze cross-repo claims across four straight headless runs did not
reproduce this time.

## /state

Revalidated STATE.md against reality:
- Engine unittest suite re-run: 210/210 green (was 164 on 2026-08-24, +46
  tests). Hook wiring: 13 modules / 17 event wirings in `.claude/settings.json`
  (was 12/16) — the new module is `artifact_sync.py`. Both counts internally
  consistent.
- Tried `~/.claude/repo-registry`, every target's `.claude/kit-version`, and
  `crontab -l` on the chance the sandbox boundary had moved — all three read
  cleanly, no harness refusal. This surfaced a fact no prior run could see:
  the registry now has SIX lines, not five — `ecosystem-kit` itself is in
  there alongside the five real targets. `kit-propagate` explicitly skips its
  own path (`tools/kit-propagate:399`) so this can't trigger a kit-into-itself
  PR, but `pr-comment-poller`/`pr-rebase`/`prune-stale-branches` carry no such
  skip, so those three now act on this repo directly. Recorded in STATE as a
  live fact, and in ISSUES as a "did not reproduce, re-test before trusting"
  note rather than a declared policy change — one green run isn't proof the
  block is gone for good.
- All five true targets' `kit-version` stamps confirmed current and mutually
  consistent (DevContainer @ `f774d17`, the other four @ `6b3a963`, all
  2026-08-30) — propagation is demonstrably working, not just configured.
- Restamped `Last validated: 2026-08-31`.

## /verify

- Engine suite re-verified 210/210 (ticked with today's date and count).
- The three items that need a live/mutating pass (session_boot banner,
  docs_contract Stop-gate, fail-open/fail-closed split) are still genuinely
  blocked — reading files mid-session, even successfully, doesn't confirm
  what renders at session START or survive deliberately breaking a hook, both
  of which are outside a read-only run's remit. Annotated the session_boot
  entry with this run's new read-access data point so it doesn't read as
  "still fully blocked" — the other two are untouched since nothing about
  them changed. Pass/fail table: 1 pass (engine suite, re-verified), 0 fail,
  3 blocked (unchanged in count, one re-scoped).

## /summary

This week's commits already carried their own CHANGELOG entries as they
landed (verified — no backfill needed). Top-3 do next:

1. **`update.sh` skip-if-locally-modified for skills** (ISSUES 2026-07-23,
   urgency raised 2026-08-01) — still open, still the highest live/automated
   risk: `kit-propagate` runs `update.sh` daily against all five real
   targets, and a project-customized skill can be silently clobbered inside
   an auto-PR with only owner review as backstop. Unchanged by this week's
   work, so it keeps floating to the top by elimination as much as urgency.
2. **Close the three stale VERIFY items with a real interactive pass**
   (session_boot banner, docs_contract Stop-gate, fail-open/fail-closed
   split) — fifth week landing here. This week's accidental sandbox access
   is a reminder these three specifically need INTERACTIVE + MUTATING
   capability, not just wider file access, so the fix is still "someone runs
   a deliberate non-headless session," not another annotation.
3. **Daemon reaper for orphaned paths** (ISSUES 2026-08-01) — small, contained
   fix (daemons for deleted worktree paths never exit and never get asked to)
   that got more relevant this week: the registry grew from five entries to
   six, which is exactly the kind of slow accretion the reaper would need to
   keep up with if the registry keeps growing.

Not top-3 but note-worthy: the `update.sh` kit-version stamp source (ISSUES
2026-07-23, `kit.config.example.json` vs. target `kit.json` fallback) is
UNRELATED to this week's `kit-propagate` stamp-read fix (2026-08-30, which
changed where `recorded_kit_commit` reads the commit hash, not where
`update.sh` reads the version number) — checked the two code paths directly
so this doesn't get marked resolved by proximity next time someone skims
CHANGELOG.

## Git

Clean, `main`, tracking `origin/main`, 0 ahead / 0 behind at run start — the
stacked-unpushed-hygiene-commit backlog flagged 2026-08-17/08-24 is cleared
(ISSUES 2026-08-24 resolved). No rebase or merge in progress, not detached —
safe to commit `.memory/` per hygiene policy.
