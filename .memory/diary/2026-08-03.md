# 2026-08-03 — weekly `.memory` hygiene (headless)

## What this run did

Ran the weekly hygiene sweep (`/state` → `/verify` → `/summary`), scoped to
this repo's `.memory/` only, as instructed for headless runs.

**`/state`.** Re-checked STATE.md's in-repo claims against reality:

- Engine unittest suite is **164/164**, not the stale 94/94 the file still
  quoted from 2026-08-01 — real growth from the shell-splitter bypass fix,
  the branch-diary commit gate, and today's PR-thread tooling, all landed
  since the last hygiene pass. Corrected in STATE.md.
- Hook wiring is still internally consistent: 12 modules in
  `engine/hooks/*.py`, 16 event wirings in this repo's own
  `.claude/settings.json` — matches the Layout section, no drift there.
- git is clean on `main`, tracking `origin/main`, HEAD `48c18a2`
  ("reply in-thread and resolve addressed review threads").

**The actual surprise this run:** trying to re-check STATE's cross-repo
bullets (five-repo rollout, live crons, `settings.local.json` wiring) hit a
hard sandbox wall — `cat ~/.claude/repo-registry` and any `~/.claude/*` read
are blocked outright by the harness, not just writes, not just other repos'
checkouts. That's new information: a routine headless hygiene run can revalidate
this repo's own git/test state but has **no way** to reconfirm the
machine-layer claims STATE.md makes, so those bullets are frozen at whatever
an interactive/wider-scoped session last verified (2026-08-01) and will read
as current indefinitely unless something else checks them. Filed as ISSUES
2026-08-03 rather than silently re-stamping "Last validated" over an
unverified claim.

**`/verify`.** All three still-open VERIFY items (session_boot banner
content, docs_contract Stop-gate behavioral pass, fail-open/fail-closed
behavioral pass) remain open — each needs either an interactive session or a
mutating action (edit a source file and attempt Stop; deliberately break a
hook file) that this read-only headless run correctly declines to perform.
Annotated with today's date so "still open" is distinguishable from "not
re-checked."

**`/summary`.** Top-3 below.

## What changed

- `STATE.md`: validation date → 2026-08-03; test count corrected 94→164;
  added a scope caveat on the cross-repo bullets; added a one-line rollup of
  everything shipped since 2026-08-01 (bypass fix, wiring propagation,
  branch-diary gate, promotion-bar rewrite, `pr-thread` tool).
- `VERIFY.md`: three open items re-annotated with 2026-08-03 status (still
  open, still for the same reasons, no regression).
- `ISSUES.md`: new entry — headless hygiene sandbox blocks all
  `~/.claude/*` reads, so cross-repo/machine claims can't be reverified from
  this kind of run.

## Top-3 do next

1. **`update.sh` skill-clobber risk (ISSUES, 2026-07-23, urgency raised
   2026-08-01).** A project-customized `templates/skills/` file gets silently
   overwritten on refresh, and `kit-propagate` now runs `update.sh` against
   every registered repo *daily* via auto-PR — the blast radius grew from
   "someone runs update.sh by hand" to "happens every night unless the owner
   catches it in review." Needs a skip-if-locally-modified mode or moving
   customizable skills out of the refresh set.
2. **Close the three stale VERIFY items with a real interactive/wider-scoped
   pass** (session_boot banner content in mylantite, docs_contract Stop-gate,
   fail-open/fail-closed split) — all three have been open since 07-23/08-01
   and none of them are reachable from a headless `.memory`-only run by
   design; they need someone to deliberately run one non-headless session
   against them.
3. **Build the smallest useful piece of the runner+CI-pipeline guidance**
   (IDEAS, 2026-08-01) — `templates/skills/ci-triage.md`, modelled on
   `fix-test-failures.md`. This is the largest pending idea but also the
   most ready to start: the field notes are already gathered in
   `.memory/references/runner-ci-field-notes.md`, two repos (mylantite,
   meritick) already need it today, which clears the promotion bar this repo
   itself just tightened.

## Git

Clean, `main`, tracking `origin/main`, 0 unpushed commits at run start.
Per hygiene policy, only `.memory/` paths get staged and committed from this
run (no rebase/merge in progress, not detached — safe to commit).
