# Diary — 2026-07-23

<!-- Dear-diary format: honest first person, written for the future me who has
lost this session's context. One "## Session" block per session; on
multi-session days, append the next block below the previous one. -->

## Session — kit v1 build + verification fix pass

Dear diary,

**Discussed:** This was the build-and-harden day for ecosystem-kit v1.0.0 —
the full SDLC ecosystem installed BY COPY into every project repo (mylantite,
grade5, meritick, homelab, DevContainer), machinery in `.claude/`, knowledge
in `.memory/`, differentiated only by a per-project profile. After authoring,
a verifier fleet (unit tests, coherence review, and a real install dry-run)
came back with 16 findings; this session was the fix pass. The recurring
theme in the findings: guarantees stated in docs that the code quietly
under-delivered — a gitignore glob that didn't match the file the code
actually writes, an installed layout that didn't survive `git clone`, a
conductor told to do what its own frontmatter forbids.

**Decided:** (all mirrored in DECISIONS/ISSUES/CONVENTIONS)
- The daemon cooldown file is `.daemon.start_attempt` — the dot after
  "daemon" is load-bearing so the existing `.daemon.*` gitignore glob covers
  it; no new snippet line, and already-installed repos are fixed by a plain
  engine refresh.
- `.memory/cache/` self-ignores via a TRACKED `cache/.gitignore` (`*` +
  `!.gitignore`); the root-level `.memory/cache/` snippet line is GONE (it
  made the mechanism untrackable — git can't re-include inside an excluded
  dir). install.sh scrubs the legacy line and seeds `.gitkeep` into empty
  `diary/`/`auto/` so the whole layout survives a clone.
- WHERE-less DELETE mirrors the WHERE-less UPDATE rule (no end-anchor);
  filesystem/git guard patterns match with quoted spans blanked so commit
  messages that merely mention `rm -rf` pass, while SQL and env-staging keep
  raw matching on purpose (quoted SQL / quoted paths are the normal shapes).
- The conductor delegates ALL write-phases to spawned subagents even without
  team tooling — it can never Edit/Write itself.
- `/decide` and `/idea` now drop their docs_contract flags FIRST, so the
  decision/discussion flag path finally has producers.

**Done:** (see CHANGELOG for the one-liner)
- Fixed all four majors: cooldown-file gitignore gap, conductor
  self-contradiction, git round-trip of the installed `.memory/` layout, and
  the snippet/cache mechanism conflict.
- Fixed the cheap minors: WHERE-less DELETE bypass, quoted-mention false
  positives, out-of-repo path matching in docs_contract + tdd_gate,
  `file_write_rules` schema discipline (defaults + example + doc), README
  ownership table, ARCHITECTURE drift (update semantics, read-only roles,
  Read wiring row), kit.config.example.md drift (stack/always_load/
  branch_types), CONVENTIONS render-vs-verbatim line, decide/idea flag steps.
- Verified: engine suite 94/94 green; fresh meritick install into a scratch
  git repo → health-check 18 OK / 2 WARN (daemon cold, no diary yet) / 0 ERR;
  commit → clone → health-check on the clone 0 ERR (the round-trip that
  failed yesterday); 8/8 behavioral hook checks (block/allow/flag/skip);
  idempotent re-run confirmed, legacy `.memory/cache/` line scrubbed.

**Open:** (tracked in ISSUES.md)
- The kit repo has ZERO commits — kit-version stamps say "kit commit
  unknown". Owner action: make the initial commit (Claude never writes to
  main/master, and an unborn `main` is exactly that).
- AF_UNIX socket path limit disables the daemon in deep checkouts (falls back
  to direct exec, safe but slower). Optional hardening: hashed /tmp socket.
- `update.sh` overwrites project-customized skills; migration blocker on this
  machine: legacy `_client.py` wirings still in `~/.claude/settings.local.json`.
- Next session: install into the five real repos, starting with a low-stakes
  one (homelab or devcontainer), and watch session_boot + docs_contract live.
