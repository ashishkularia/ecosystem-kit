# Diary — 2026-08-01

<!-- Dear-diary format: honest first person, written for the future me who has
lost this session's context. One "## Session" block per session; on
multi-session days, append the next block below the previous one. -->

## Session — weekly .memory hygiene (headless)

Dear diary,

**What the hygiene run found:** STATE.md was stale in the happy direction —
it still said "kit not yet installed anywhere, rollout blocked on the legacy
`~/.claude/settings.local.json` wiring", while reality is that the rollout
COMPLETED: all five registered repos (DevContainer, its nested
mylantite/grade5/meritick checkouts, and homeassistant on the homelab
profile) carry kit v1.0.0 stamped from real kit commits (7e0d2d8 / 59916b5),
all on the `$CLAUDE_PROJECT_DIR` wiring, and the legacy `_client.py` wirings
in `~/.claude/settings.local.json` are gone (only the allowed machine-level
branch guard remains). Two geography surprises checked out benign: the
project repos live NESTED under `/home/ubuntu/DevContainer/` (daemon socket
paths still ~60 chars, far under the AF_UNIX limit), and
`/home/ubuntu/grade5` is just a convenience symlink, not a drifting second
install. Engine suite re-run: 94/94 green. Live-session hook artifacts
(tdd_state/pending/context_attach) exist in mylantite+meritick caches, so
the engine demonstrably runs in real sessions.

**What changed:**
- STATE.md revalidated (stamp 2026-07-23 → 2026-08-01): rollout status
  rewritten, registered-repo geography corrected, `tools/` machine layer
  added to Layout, test count 90→94.
- VERIFY.md: guard_protected_merge ticked — verified by piping stdin-JSON
  fixtures at the real hook from this on-main checkout (merge on main,
  `gh pr merge`, push refspec to main, MCP merge_pull_request, MCP
  create_or_update_file on main all exit 2; feature-push control exits 0;
  6/6). The three remaining entries (session_boot banner, docs_contract
  stop-gate, fail-open/fail-closed) annotated: not checkable read-only from
  a hygiene run — they need an interactive/behavioral session.
- ISSUES.md: two RESOLVED (kit-repo-has-zero-commits; the
  settings.local.json migration blocker), skills-overwrite issue annotated
  with raised urgency (kit-propagate now runs update.sh daily → silent
  clobber risk inside auto-PRs), daemon path-length issue annotated with the
  corrected repo geography.

**Top-3 do next:**
1. Decide the `update.sh` skills-refresh policy (ISSUES 2026-07-23):
   skip-if-locally-modified or move customizable skills out of the refresh
   set — most urgent open issue now that kit-propagate ships update.sh into
   every repo daily.
2. Run the three outstanding VERIFY behavioral checks in an interactive
   session (session_boot banner content in mylantite, docs_contract
   block-then-release, fail-open/fail-closed split on a scratch install) —
   the rollout prerequisite they were waiting on is now met.
3. Stand up kit CI (IDEAS 2026-07-23): unittest suite + fixture
   install/update/health-check round-trip on every kit PR — the natural
   red-gate now that kit-propagate auto-generates PRs; fold in the single
   VERSION source fix (ISSUES 2026-07-23, kit-version stamp drift) while
   touching the release path.
